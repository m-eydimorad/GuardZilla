import { StyleSheet } from 'react-native'
const styleOfApp = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#51B6A4',
    },
});
export default styleOfApp;